# Security

Please report vulnerabilities privately to security@farfield.dev. Do not open a public issue for credential exposure, authorization bypasses, cross-tenant access, unsafe object-key handling, or corruption that can be acknowledged as durable.

The current pre-release CLI does not provide authentication or tenant isolation. Deploy it only in a trusted environment and use least-privilege object-storage credentials.
